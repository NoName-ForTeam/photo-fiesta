import { Typography } from '@photo-fiesta/ui-lib'

//TODO: change to 404 page
const NotFound = () => {
  return (
    <div
      style={{ alignItems: 'center', display: 'flex', height: '100vh', justifyContent: 'center' }}
    >
      <Typography variant={'textLarge'}>PAGE 404</Typography>
    </div>
  )
}

export default NotFound
